version: '3.8'

services:
  backend:
    build:
      context: ./server  
    image: rajaramachandran/eventdesk:Stripe_test_backend
  frontend:
    build:
      context: ./frontend 
    image: rajaramachandran/eventdesk:Stripe_test_frontend